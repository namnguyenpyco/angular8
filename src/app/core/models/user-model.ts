export interface UserInfo {
  avatarUrl: string,
  company: string,
  email: string,
  id: string,
  message: string,
  position?: string[],
  project?: string[],
  skill?: string[],
  token?: string,
  status: number,
}
